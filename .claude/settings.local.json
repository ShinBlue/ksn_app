{
  "permissions": {
    "allow": [
      "Bash(flutter pub:*)",
      "Bash(git --version && git config user.name && git config user.email)",
      "Bash(ssh -T git@github.com 2>&1)",
      "Bash(git add:*)"
    ]
  }
}

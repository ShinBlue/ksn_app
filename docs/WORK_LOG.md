# 作業ログ（ksn_app）

2026年6月頃の開発セッションで行った作業内容のまとめ。休眠再開時の参照用。

---

## 目次

1. [すごろくゲーム](#1-すごろくゲーム)
2. [カルタゲーム（新規実装）](#2-カルタゲーム新規実装)
3. [デプロイ](#3-デプロイ)
4. [既知の注意点・修正履歴](#4-既知の注意点修正履歴)
5. [次にやるとよいこと](#5-次にやるとよいこと)
6. [関連コミット（時系列）](#6-関連コミット時系列)
7. [2026-07 セッション作業まとめ](#7-2026-07-セッション作業まとめ)
8. [2026-07-10 セッション作業まとめ](#8-2026-07-10-セッション作業まとめ)

---

## 1. すごろくゲーム

### 概要

メインメニューから「すごろく」を選び、盤面・進み方・コマを設定して2人で遊べる。

### ファイル構成

| パス | 役割 |
|------|------|
| `lib/sugoroku/sugoroku_models.dart` | 盤面サイズ、進み方、コマ、マス定義 |
| `lib/sugoroku/sugoroku_boards.dart` | デフォルトの特殊マス配置 |
| `lib/sugoroku/sugoroku_board_layout.dart` | 散らしたマス座標・矢印描画 |
| `lib/sugoroku/sugoroku_board_view.dart` | 進行用盤面UI（枠付き） |
| `lib/sugoroku/sugoroku_movement.dart` | 移動・マス効果・ゴール判定 |
| `lib/sugoroku/sugoroku_piece_catalog.dart` | コマ候補 |
| `lib/sugoroku/sugoroku_animals.dart` | 動物盤データ（20マス/10マス） |
| `lib/sugoroku/sugoroku_animal_board_view.dart` | 動物選択盤UI |
| `lib/sugoroku/sugoroku_heart_number.dart` | 数字当て用ハート数字 |
| `lib/sugoroku/sugoroku_screens.dart` | セットアップ〜ゲーム画面 |
| `assets/images/動物/` | 20匹の個別イラスト（ひらがなファイル名） |
| `assets/images/盤面/動物盤面.png` | 20マス用合成盤面（1024×570） |
| `tool/crop_sugoroku_animals.py` | 合成盤面から個別画像を切り出すスクリプト |
| `assets/images/盤面/` | 動物盤面画像など |

### ゲームフロー

1. **盤面の長さ** … 10マス / 20マス
2. **セットアップ画面（1画面）** … 左:コマ / 中央:盤面+スタート / 右:進み方
3. **スタート** … 画面遷移なしで同画面のままプレイ開始
4. **プレイ中** … 左:コマ状態 / 中央:盤面 / 右:進み方操作

### 進み方（4種類）

| モード | 内容 |
|--------|------|
| サイコロ | 目の数だけ進む |
| じゃんけん | グー→2、パー→3、チョキ→2 |
| 動物を選ぶ | どうぶつをえらんで、なまえのかずだけすすむ |
| 数字当て | すうじをタップしてどうぶつをあてて、かずだけすすむ |

※ UI 文言は幼児向けにひらがな中心。**スタート・ゴール**のみカタカナ表記。

### 動物モード・レイアウト（2026-07-10 更新）

| 盤面 | 動物の表示場所 | 備考 |
|------|----------------|------|
| **10マス** | 中央盤面の**下**（従来どおり） | 2列×5行のカードグリッド |
| **20マス** | 右パネル（188px）の**操作パネル下** | 中央はすごろく盤のみで大きく表示 |

**20マス右パネル構成**

```
┌─────────────────┐
│ 動物を選ぶ      │  ← 既存の _GameControlPanel（そのまま）
│ ○○のばんです    │
├─────────────────┤
│ 🐷 🦝          │  ← 2列×10行、スクロールなし
│ 🐨 🐐          │     高さに合わせてアイコン自動縮小
│  …              │
└─────────────────┘
```

- `AnimalBoardLayout.sidePanel` … 右パネル用コンパクトグリッド
- `_AnimalSidePanelGrid` … `LayoutBuilder` でセルサイズを動的計算
- 10マスは `AnimalBoardLayout.standard` のまま

### 動物画像アセット（2026-07-10 更新）

| 項目 | 内容 |
|------|------|
| 個別画像 | `assets/images/動物/{ひらがな名}.jpg` × 20匹 |
| 合成盤面 | `assets/images/盤面/動物盤面.png`（5列×4行） |
| 切り出し | `python3 tool/crop_sugoroku_animals.py` で再生成可 |
| 登録 | `SugorokuAnimals.imageByName` に20匹すべて登録 |

切り出し時の注意: 均等グリッドでは隣の動物が混ざるため、ぶた・こあら・やぎ等は座標を個別調整。

### 幼児向け文言（2026-07-10 更新）

| 変更前 | 変更後 |
|--------|--------|
| 一回休み | おやすみ |
| 一個進む | ひとつすすむ |
| 動物を選ぶ | どうぶつをえらぶ |
| 数字当て | すうじあて |
| ○○の番です | ○○のばんです |
| 青丸・赤丸… | あおまる・あかまる… |
| スタート / ゴール | **カタカナのまま**（ユーザー指定） |

対象ファイル: `sugoroku_boards.dart`, `sugoroku_movement.dart`, `sugoroku_models.dart`, `sugoroku_piece_catalog.dart`, `sugoroku_screens.dart` ほか

### 数字当て（2026-07-10 更新）

- ハート上の数字色を **常に濃いグレー**（`#263238`）に統一
- 淡いハート色でも読みやすくするため
- 実装: `lib/sugoroku/sugoroku_heart_number.dart` の `HeartNumberBadge`

### じゃんけん（2026-07 更新）

- 説明文: `グー・チョキは2マス、パーは3マス`（「ぐみ・パイン・ハサミ」表記は削除）
- プレイ中メッセージ: `グー（2マス）。画面をタップ` 形式

### 盤面レイアウト（2026-07 更新）

| 項目 | 内容 |
|------|------|
| 盤面サイズ | 横幅1.5倍・縦1.2倍（`boardWidthScale` / `boardHeightScale`） |
| 10マス最大幅 | 600px |
| 20マス最大幅 | 720px |
| マス配置 | 10/20マスとも座標を見直し、重なりを解消 |
| 文字サイズ | 10マス 15px / 20マス 13px（`FittedBox` で収める） |
| 画面フィット | `boardWidthFitting()` で利用可能な幅・高さに自動スケール |
| スクロール | 盤面エリアはスクロールなし。左右パネルのみ必要時スクロール |

### カスタムマス（2026-07 更新）

| 操作 | 見た目 |
|------|--------|
| ＋で文字を入力（デフォルトと異なる） | マスが **黄色** |
| 「デフォルト」で元に戻す | **青**（通常マス）に戻る |
| 4文字以上のカスタム文字 | マウスオーバーで吹き出し表示（文字サイズはマス内と同じ） |

- 判定: `SugorokuBoardDefaults.defaultCellAt()` のラベルと比較
- 実装: `lib/sugoroku/sugoroku_board_view.dart` の `_BoardCell`

### 関連コミット

- `804da4b` … すごろくゲーム追加・GitHub Pages デプロイ更新（`main`）
- `cfc8281` … 盤面拡大・じゃんけん表記整理・マス配置見直し
- `069f9fa` … 盤面の画面内自動スケール
- `bdb4da8` … カスタムマス黄色・4文字以上ホバー拡大
- `3d0586c` … ホバー文字サイズをマス内と統一
- `0cf4122` … 動物20匹・右パネルレイアウト・幼児向け文言・数字当て視認性（2026-07-10）

---

## 2. カルタゲーム（新規実装）

### 概要

メインメニュー「カルタ」から、50音表で文字を選び、選んだカードを1画面に表示するゲーム。

- **選択画面** … プリセット・50音表・並べ方・ゲーム開始
- **ゲーム画面** … 選択カードを全件1画面表示（整列 or ランダム配置）

### ファイル構成

| パス | 役割 |
|------|------|
| `lib/karuta/karuta_models.dart` | `KarutaCard`, `KarutaRow`, `KarutaLayoutMode`, `KarutaDisplayLine` |
| `lib/karuta/karuta_catalog.dart` | 文字リスト、行定義、50音表表示行、画像パス |
| `lib/karuta/karuta_repository.dart` | CSV読み込み・カード生成 |
| `lib/karuta/karuta_selection_screen.dart` | 50音選択UI（プリセット・右パネル） |
| `lib/karuta/karuta_game_screen.dart` | ゲーム画面（シャッフル・タップで詳細） |
| `lib/karuta/karuta_grid_layout.dart` | 整列グリッド配置 |
| `lib/karuta/karuta_scatter_layout.dart` | ランダム散らし配置 |
| `lib/main_menu_screen.dart` | メニューからカルタ選択画面へ遷移 |
| `assets/data/karuta_sentences.csv` | 読み札テキスト（63行） |
| `assets/images/カルタデータ/{文字}.png` | カルタ画像（71枚） |

### データ設計

#### 文字の分類

| 区分 | 件数 | 内容 |
|------|------|------|
| 清音 | 46 | `karutaSeionCharacters` |
| 濁音のみ | 18 | `karutaDakutenCharacters`（半濁音・ぢづ除く） |
| 濁音＋半濁音 | 23 | `karutaVoicedCharacters` |
| 全部（ゲーム対象） | 69 | 清音46 + 濁半濁23（**ぢ・づは除外**） |
| CSV読み札あり | 63 | `karutaCsvCharacterOrder` |
| 画像のみ（読み札なし） | 8 | かきくけ・ざじずぜ |

#### 画像パス

```
assets/images/カルタデータ/{文字}.png
```

#### CSV

- `assets/data/karuta_sentences.csv`
- `pubspec.yaml` にアセット登録済み
- 読み札がない文字は `sentence: null` でカード表示（詳細シートでは読み札欄非表示）

### 選択画面 UI

#### 全体レイアウト

```
┌─────────────────────────────┬──────────┐
│ プリセット（1行・横スクロール可）      │          │
│ （18px 余白）                          │  右パネル │
│ 50音表（11行・スクロールなし）         │  ○こえらび│
│  か行｜が行 のように清濁ペア            │  ならべかた│
│  ぱ行はば行の真下（右列揃え）           │  整列/ランダム│
│                                         │  ゲーム開始│
└─────────────────────────────┴──────────┘
```

- **左**: プリセット + 50音表（`karutaDisplayLines`）
- **右**: 操作パネルのみ参考デザインに合わせてコンパクト化（プレビュー文字一覧なし）

#### プリセット（トグル式）

| ボタン | 内容 |
|--------|------|
| あいうえお | 5文字 |
| 清音だけ | 46文字 |
| 濁音だけ | 18文字 |
| 濁音・半濁音 | 23文字 |
| 全部 | 69文字 |
| 全部解除 | 常時クリア（トグルではない） |

- フォント 12px、パディング 横12/縦8
- プリセットとあ行の間は **18px**（当初6pxの3倍）

#### 50音表の表示ルール

- 清音と濁音は **同一行にペア表示**（例: か行｜が行）
- 濁音行は清音行の右に配置（`pairGap` で間隔）
- **ぱ行** は **ば行の真下**、右列（濁音列）に揃え（`alignToSecondaryColumn: true`）
- **だ行** は `characterSlots: [0, 3, 4]` で **だ・で・どのみ** 表示（ぢ・づは非表示）
- 各行左端揃え、画面高に合わせてスケール（`KarutaSelectionMetrics.fit`）

#### 右パネル

1. `○こ えらび`（選択数）
2. `ならべかた`
3. **整列** / **ランダム**（縦並びトグル）
4. **ゲーム開始**（ランダムの直下）

### ゲーム画面

| 項目 | 内容 |
|------|------|
| カード順 | 開始時にシャッフル |
| 整列モード | `karuta_grid_layout.dart` … グリッドで1画面に収める |
| ランダムモード | `karuta_scatter_layout.dart` … ランダム座標で散らし |
| カード表示 | `BoxFit.contain`（上端切れ対策済み） |
| タップ | ボトムシートで文字・画像・読み札表示 |
| Analytics | `karuta_game_view`（card_count, layout_mode） |

### 画面遷移

```
メインメニュー
  └─ カルタ（KarutaSelectionScreen）
       └─ ゲーム開始 → KarutaGameScreen
```

- Route: `/karuta/select`, `/karuta/game`
- Analytics: `logMainMenuSelect('karuta')`

### 修正した問題（カルタ）

| 問題 | 対応 |
|------|------|
| カード上端が切れる | `BoxFit.cover` → `BoxFit.contain` |
| 50音表の横オーバーフロー（約10px） | メトリクス計算のギャップと描画ギャップを統一（5px） |
| プリセット過大 | フォント11px・1行Row（狭い画面は横スクロール） |
| 全体レイアウトを誤って変更 | 50音表は左カラムのまま、**右カラムのみ**参考デザインに合わせて復元 |
| プリセットとあ行が近い | 間隔 6px → 18px、ボタンをやや拡大（12px/パディング増） |

---

## 2b. まるばつ・ことば盤面（2026-07 更新）

### 問題

テキスト盤面（4パターン）の修正が保存されない。パターン選択のたびにデフォルトで上書きされていた。

### 対応

| 変更 | 内容 |
|------|------|
| 保存方式 | パターンごとに独立キー（例: `board_text_pattern1_0`） |
| パターン選択 | 保存済み内容を読み込むだけ（上書きしない） |
| 編集UI | ホームの鉛筆削除 → **パターン選択画面**で各パターンを編集 |
| 初期値復元 | パターン単位で「初期の盤面にもどす」 |
| 編集画面ボタン | 「変更を保存する」「初期の盤面にもどす」をわかりやすく配置 |

### 主要ファイル

- `lib/board_content_store.dart` … `labelsForTextPattern` / `saveTextPatternLabels` / `resetTextPattern`
- `lib/text_pattern_select_screen.dart` … 各パターンに編集・再生ボタン
- `lib/board_edit_screen.dart` … 編集・保存・初期値復元UI
- `lib/home_screen.dart` … テキスト盤の鉛筆アイコン削除

### 関連コミット

- `6d14042` … ことば盤面をパターン別に保存し、編集UIを改善

---

## 3. デプロイ

詳細は `docs/DEPLOY.md` を参照。

| 環境 | 用途 | URL | 方法 |
|------|------|-----|------|
| **テスト** | クライアント確認 | https://shinblue.github.io/ksn_app/ | `main` push → **自動** |
| **本番** | 公開 | https://ksn-apps.web.app | Actions **手動** |

### ワークフロー

- `.github/workflows/deploy.yml` … GitHub Pages（Staging）
- `.github/workflows/firebase-production.yml` … Firebase Hosting（Production）

### 日常フロー

```
1. コード修正
2. main へ push
3. テスト URL で確認
4. OK なら本番デプロイ（手動）
```

### 2026-07 に実施したデプロイ

| 日付 | 環境 | コミット例 | 内容 |
|------|------|-----------|------|
| 2026-07-08 | テスト | `6d14042` | ことば盤面パターン別保存 |
| 2026-07-08 | 本番 | `6d14042` 時点 | Firebase CLI で `ksn-apps` へデプロイ |
| 2026-07-08 | テスト | `069f9fa` | すごろく自動スケール |
| 2026-07-08 | テスト | `bdb4da8` | カスタムマス黄色・ホバー |
| 2026-07-09 | テスト | `3d0586c` | ホバー文字サイズ統一 |
| 2026-07-10 | テスト | `0cf4122` | すごろく動物モード・20匹画像・幼児向け文言 |

### GitHub Pages 初回設定

リポジトリ **Settings → Pages → Source** を **GitHub Actions** に設定すること。

---

## 4. 既知の注意点・修正履歴

### すごろく

| 問題 | 対応 |
|------|------|
| コマ移動で盤面がずれる | 左右パネル高さ固定・タップ案内の常時スペース・上揃えレイアウト |
| 20マス盤の RenderFlex overflow | マス座標インセット・clip・aspectRatio 0.72・cellFraction 0.11 |
| 2コマ同マスで overflow | FittedBox・2コマ時は小さめ表示 |
| スタート後に画面遷移 | `SugorokuGameSetupScreen` 1画面で `_isPlaying` 切替 |

### すごろく（2026-07 追記）

| 問題 | 対応 |
|------|------|
| 盤面拡大後にスクロールが必要 | 画面サイズに合わせて盤面を自動スケール |
| マスが重なる | マス中心座標の見直し |
| カスタム文字が小さく読みにくい | フォントサイズ拡大 + 4文字以上はホバー表示 |

### すごろく（2026-07-10 追記）

| 問題 | 対応 |
|------|------|
| 10マス「動物を選ぶ」で画像が出ない | `imageByName` を20匹に拡張、個別 `.jpg` を追加 |
| 合成画像からの切り出しが不正確 | `tool/crop_sugoroku_animals.py` で最大連結成分＋個別座標調整 |
| 20マス盤・動物盤が小さい | 動物を右パネルへ移し中央盤面を全幅・全高で表示 |
| 右パネル動物がスクロール必要 | `_AnimalSidePanelGrid` で高さに合わせセル自動縮小 |
| 数字当ての数字が見えにくい | 数字色を濃いグレーに統一 |
| 漢字が幼児に難しい | ゲーム内文言をひらがな中心に（スタート・ゴールはカタカナ） |

### 開発環境（2026-07-10 追記）

| 問題 | 内容 |
|------|------|
| `flutter run` が起動しない | Xcode 初回セットアップ未完了（`xcodebuild -checkFirstLaunchStatus` exit 69）、`devicectl` ハング等 |
| 対処 | `sudo xcodebuild -license accept`、`pkill` でハングプロセス終了、`sudo xcodebuild -runFirstLaunch` の実行を推奨 |

### まるばつ・ことば盤面

| 問題 | 対応 |
|------|------|
| テキスト盤の修正が保存されない | パターン別保存・選択時に上書きしない |
| 4パターンを個別編集できない | パターン選択画面から編集 |

### カルタ

| 問題 | 対応 |
|------|------|
| カード上端切れ | `BoxFit.contain` |
| 50音表 overflow | ギャップ値の統一・ペア行に `FittedBox` + 幅固定 |
| 右パネルが大きすぎる | プレビュー削除・コンパクトColumn |

---

## 5. 次にやるとよいこと

### すごろく

- [x] 盤面の画面内フィット（2026-07 対応済み）
- [x] カスタムマスの色分け・長文ホバー（2026-07 対応済み）
- [x] 20マス動物選択の右パネル化・20匹画像（2026-07-10 対応済み）
- [x] 幼児向けひらがな文言（2026-07-10 対応済み）
- [ ] 実機・タブレットでのレイアウト確認（20マス右パネル・10マス下配置）
- [ ] 切り出し画像の目視最終確認（全20匹）
- [ ] マスの効果も編集できるようにする（任意）
- [ ] 3人以上対応（任意）

### まるばつ・ことば盤面

- [x] パターン別保存・編集UI（2026-07 対応済み）
- [ ] テスト・本番での動作最終確認

### カルタ（優先）

- [ ] 読み音声アセットの追加・再生（`assets/sounds/karuta/{文字}.wav` 等）
- [ ] カルタ本体の遊び方（取り合い・読み上げ連動など）の実装
- [ ] 実機・タブレットでの50音表レイアウト確認
- [ ] 読み札なし8文字（かきくけ・ざじずぜ）のCSV追加検討

### 共同開発

- [ ] `dev/tatsuya` を `main` の最新（すごろく修正含む）に更新
- [ ] GitHub で `tatsuya00011` を Collaborator 招待（未実施なら）

### デプロイ

- [x] すごろく動物モード等をテスト環境へ反映（`0cf4122`、2026-07-10）
- [ ] テスト URL で20マス・10マス・数字当てを確認
- [ ] 問題なければ本番 Firebase へ反映
- [ ] 本番 URL で最終確認

### 開発環境

- [ ] Flutter SDK アップグレード（`pubspec.lock` を `git restore` してから `flutter upgrade`）

---

## 6. 関連コミット（時系列）

| コミット | 内容 |
|---------|------|
| `804da4b` | すごろくゲーム追加・GitHub Pages デプロイ更新 |
| `26c6c54` | カルタゲーム実装（50音選択・ゲーム画面・CSV・メニュー連携） |
| `6d14042` | ことば盤面をパターン別に保存し、編集UIを改善 |
| `cfc8281` | すごろく盤面のレイアウトとじゃんけん表示を改善 |
| `069f9fa` | すごろく盤面を画面内に収まるよう自動スケール |
| `bdb4da8` | すごろくのカスタムマス黄色表示・長文ホバー |
| `3d0586c` | すごろくのホバー表示文字サイズをマス内と統一 |
| `0cf4122` | すごろく動物20匹・右パネルレイアウト・幼児向け文言・数字当て視認性 |

---

## 7. 2026-07 セッション作業まとめ

### 実施した機能変更

1. **ことば盤面（まるばつ）** … 4パターン別保存、パターン選択画面から編集、初期値復元
2. **すごろく** … 盤面拡大・マス配置改善・画面内フィット・じゃんけん表記整理
3. **すごろく** … カスタムマス黄色、4文字以上はマウスオーバーで全文表示

### Git・共同開発

| 項目 | 内容 |
|------|------|
| 作業ブランチ作成 | `dev/tatsuya`（`main` から作成・push 済み） |
| 用途 | `tatsuya00011` さんと共同編集用（`main` は触らせない運用） |
| 注意 | `dev/tatsuya` はその後 `main` が進んだため **更新が必要** |

### デプロイ状況（2026-07-09 時点）

| 環境 | URL | 最新反映 |
|------|-----|---------|
| テスト | https://shinblue.github.io/ksn_app/ | `3d0586c` まで反映済み |
| 本番 | https://ksn-apps.web.app | `6d14042` 時点（すごろく修正は未反映） |

### 開発環境メモ

**Flutter upgrade エラー**（Flutter SDK 本体の `pubspec.lock` が変更されている）:

```bash
cd /Users/shinyasaki/development/flutter
git restore pubspec.lock
flutter upgrade
```

### 次にやること（優先順）

1. テスト環境ですごろく・ことば盤面を確認
2. 問題なければ本番 Firebase デプロイ
3. `dev/tatsuya` を最新 `main` に追従
4. Collaborator 招待（未実施なら）

---

## 8. 2026-07-10 セッション作業まとめ

### 実施した機能変更

1. **動物画像アセット**
   - 合成盤面（1024×570）から20匹の個別 `.jpg` を生成
   - `tool/crop_sugoroku_animals.py` を追加（再生成用）
   - `SugorokuAnimals.imageByName` を20匹すべてに拡張

2. **20マス盤レイアウト**
   - 中央: すごろく盤のみ（拡大表示）
   - 右パネル（188px）: 既存操作パネル + その下に動物2列グリッド
   - 10マス盤は従来どおり盤面下に動物カード

3. **右パネル動物グリッド**
   - スクロールなしで20匹すべて表示
   - 利用可能高さに応じてアイコン・文字を自動縮小（`_AnimalSidePanelGrid`）

4. **数字当て**
   - ハート上の数字を濃いグレー（`#263238`）に統一

5. **幼児向け文言**
   - すごろく内の表示をひらがな中心に変更
   - 例外: **スタート・ゴール** はカタカナ表記を維持

### レイアウト試行の経緯

| 段階 | 内容 | 結果 |
|------|------|------|
| 1 | 動物盤を盤面の下に縦配置 | 盤面・動物とも小さい |
| 2 | 盤面右に動物盤を横並び（中央エリア内） | 盤面幅が伸びにくい |
| 3 | 操作ボタンを動物Columnに移動 | ユーザー意図と不一致 |
| **4（確定）** | 右パネル操作はそのまま、下に動物グリッド | 採用 |

### デプロイ

| 日付 | 環境 | コミット | 内容 |
|------|------|---------|------|
| 2026-07-10 | テスト | `0cf4122` | 上記すべてを `main` push → GitHub Pages 自動デプロイ |

- 確認 URL: https://shinblue.github.io/ksn_app/
- 本番（Firebase）は **未デプロイ**

### 未コミット（作業ログのみ）

- `docs/WORK_LOG.md` … 本セッションの記録（この更新）

### 次にやること

1. テスト環境で20マス「どうぶつをえらぶ」「すうじあて」を確認
2. 10マス盤の動物表示・レイアウト確認
3. 問題なければ本番 Firebase デプロイ（Actions 手動）
4. iOS 実機実行時は Xcode 初回セットアップ完了を確認

---

*最終更新: 2026-07-10（すごろく動物モード・幼児向け文言・テストデプロイ）*
